
from setuptools import setup, find_packages
setup(
    name = 'external_sql_simple_project',
    version = '0.0.1',
    packages = ['external_sql_simple_project'],
    package_dir = {'external_sql_simple_project': '.'},
    description = '',
    install_requires = [],
)

